= simple_form_for @user, :url => sessions_path, :html => {:class => "form-horizontal"} do |f|
  - if flash[:alert]
    .alert.alert-error
      = flash[:alert]
  = f.input :email
  = f.input :password

  .form-actions
    = f.button :submit, t(".buttons.login")
